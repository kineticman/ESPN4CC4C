# === Resolver base (used to build links shown in playlists/redirects) ===
# Use your LAN IP so other devices can reach it.
VC_RESOLVER_BASE_URL=http://YOUR_LAN_IP:8094

# Timezone
TZ=America/New_York

# === Paths inside the container ===
DB=/app/data/eplus_vc.sqlite3
OUT=/app/out/epg.xml
VC_M3U_PATH=/app/out/virtual_channels.m3u

# === Planner settings ===
VALID_HOURS=72
LANES=40
ALIGN=30
MIN_GAP_MINS=30
SCHEDULE_HOURS=6

# (Optional) Listening port for the API.
# If unset, docker-entrypoint will derive it from VC_RESOLVER_BASE_URL (fallback 8094).
# PORT=8094

# === Chrome Capture (cc4c) ===
CC_HOST=192.168.86.72
CC_PORT=5589

# === M3U grouping (quote values with spaces) ===
M3U_GROUP_TITLE="ESPN+ VC"
