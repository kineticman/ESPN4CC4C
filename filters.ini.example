# ESPN4CC4C Filtering Configuration
# 
# Use "*" or "all" to include everything (no filtering)
# Use comma-separated values to whitelist specific items
# Uncomment and modify lines below to activate filters

[filters]
# ============================================================================
# NETWORK FILTERING
# ============================================================================
# Available networks: ESPN, ESPN2, ESPN+, ESPNU, ESPNews, ACCN, SEC Network, etc.
#
# Examples:
#   enabled_networks = ESPN,ESPN2,ESPN+
#   exclude_networks = ESPN Deportes
enabled_networks = *
exclude_networks =

# ============================================================================
# SPORT FILTERING
# ============================================================================
# Available sports: Football, Basketball, Baseball, Soccer, Hockey, etc.
#
# Examples:
#   enabled_sports = Football,Basketball,Soccer
#   exclude_sports = Dogs,Jai Alai
enabled_sports = *
exclude_sports =

# ============================================================================
# LEAGUE FILTERING
# ============================================================================
# Available leagues: NFL, NBA, MLB, NCAA Football, NCAA Basketball, etc.
# Uses partial matching by default (e.g., "NCAA" matches "NCAA Football")
#
# Examples:
#   enabled_leagues = NFL,NBA,NHL
#   exclude_leagues = College
#   enabled_leagues = NCAA  # Matches any league with "NCAA" in name
enabled_leagues = *
exclude_leagues =

# ============================================================================
# PACKAGE FILTERING (ESPN+ subscription requirements)
# ============================================================================
# Examples:
#   require_espn_plus = true        # Only show ESPN+ content
#   require_espn_plus = false       # Exclude ESPN+ content
#   exclude_ppv = true              # Exclude PPV events
require_espn_plus =
exclude_ppv = false

# ============================================================================
# RE-AIR EVENT FILTERING
# ============================================================================
# Re-Air events are replays that may have authentication issues on some devices
# (especially Android/Fire TV). Set to true to exclude them.
#
# Statistics from your database:
#   - ~7.7% of events are Re-Airs
#   - Networks with most Re-Airs: ESPNews, ACCN, ESPNU, ESPN2
#
# Examples:
#   exclude_reair = true            # Only show live/first-run content
#   exclude_reair = false           # Include re-airs (default)
exclude_reair = true

# ============================================================================
# EVENT TYPE FILTERING
# ============================================================================
# Available types: LIVE, UPCOMING, OVER, REPLAY
#
# Examples:
#   enabled_event_types = LIVE,UPCOMING    # No replays
#   exclude_event_types = OVER             # Same as above
enabled_event_types = *
exclude_event_types = 

# ============================================================================
# LANGUAGE FILTERING
# ============================================================================
# Available languages: en, es, etc.
#
# Examples:
#   enabled_languages = en          # English only
#   exclude_languages = es          # No Spanish broadcasts
enabled_languages = *
exclude_languages = es

# ============================================================================
# ADVANCED OPTIONS
# ============================================================================
# Case insensitive matching for all text filters
case_insensitive = true

# Allow partial matching for league names (e.g., "NCAA" matches "NCAA Football")
partial_league_match = true

# Exclude studio shows, talk shows, and news programs (events with no sport)
# These often don't have valid deeplinks
exclude_no_sport = false
