# --- Service ---
PORT=8094
TZ=America/New_York

# --- Container paths (bind mounts map host -> /app/*) ---
# These are read by the container; keep them as /app/* on Windows.
DB=/app/data/eplus_vc.sqlite3
OUT=/app/out
LOGS=/app/logs

# Optional: where to write the M3U (container path)
VC_M3U_PATH=/app/out/playlist.m3u

# --- Planner tunables ---
VALID_HOURS=72
LANES=40
ALIGN=30
MIN_GAP_MINS=30

# --- Resolver base URL (LAN reachable from Channels) ---
# Replace YOUR_LAN_IP with the host’s LAN IP running this container.
VC_RESOLVER_BASE_URL=http://YOUR_LAN_IP:8094

# --- Chrome Capture integration (use the SAME host IP) ---
CC_HOST=YOUR_LAN_IP
CC_PORT=5589

# --- Appearance ---
M3U_GROUP_TITLE='ESPN+ VC'

# --- Optional ESPN Watch API key (used by ingest on Windows bootstrap) ---
WATCH_API_KEY=0dbf88e8-cc6d-41da-aa83-18b5c630bc5c
