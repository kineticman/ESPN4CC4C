# --- API stack (versions aligned) ---
fastapi==0.120.0
starlette==0.48.0
uvicorn[standard]==0.38.0

# --- Core deps FastAPI relies on (pin for reproducibility) ---
anyio==4.11.0
h11==0.16.0
sniffio==1.3.1
pydantic==2.12.3
pydantic_core==2.41.4
typing_extensions==4.15.0
idna==3.11

# --- Your app uses these ---
requests==2.32.5
urllib3==2.2.2          # (compatible with requests 2.32.x)
aiofiles==24.1.0        # for StaticFiles async I/O
python-dateutil==2.9.0.post0
lxml==6.0.2             # faster XMLTV generation
tzdata==2025.2          # timezone DB inside container

# (intentionally removed: annotated-doc, typing-inspection)
