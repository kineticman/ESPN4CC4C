# =========================
# MUST UPDATE THESE FIRST
# =========================
# Your timezone (IANA format, e.g., America/New_York, Europe/London)
TZ=America/New_York

# Resolver base URL (must be reachable by Channels DVR)
# Replace YOUR_LAN_IP with the LAN IP of the host running this stack
VC_RESOLVER_BASE_URL=http://YOUR_LAN_IP:8094

# Chrome Capture integration (use the SAME host IP)
CC_HOST=YOUR_LAN_IP
CC_PORT=5589

# =========================
# UNLIKELY YOU'LL EVER TOUCH BELOW
# =========================

# --- Service ---
PORT=8094

# --- Host paths (container maps these into /app/*) ---
# Leave unset to use defaults (./data ./out ./logs), or uncomment to override.
# DB=./data/eplus_vc.sqlite3
# OUT=./out
# LOGS=./logs
# (Optional) If another tool expects a specific M3U file path:
# VC_M3U_PATH=./out/virtual_channels.m3u

# --- Planner tunables ---
# Hours of schedule to build (72 is a good default)
VALID_HOURS=72
# Number of virtual channels/lanes to maintain
LANES=40
# Align start times to minute grid (e.g., 30 -> :00/:30)
ALIGN=30
# Minimum gap in minutes between slots
MIN_GAP_MINS=30

# --- M3U appearance ---
M3U_GROUP_TITLE='ESPN+ VC'

# --- FastAPI app module (legacy/compat only) ---
APP_MODULE=bin.vc_resolver:app

# --- Optional ESPN Watch API key (if you use ingest scripts) ---
WATCH_API_KEY=0dbf88e8-cc6d-41da-aa83-18b5c630bc5c
