# =============================================================================
# ESPN4CC4C Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# ⚠️  MUST UPDATE - Replace these with your actual values
# -----------------------------------------------------------------------------

# Your computer's local IP address (find with 'ipconfig' on Windows or 'hostname -I' on Linux)
# This must be accessible by Channels DVR on your network
VC_RESOLVER_BASE_URL=http://YOUR_LAN_IP:8094
CC_HOST=YOUR_LAN_IP

# Your timezone (use IANA format: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
TZ=America/New_York

# -----------------------------------------------------------------------------
# Basic Settings (good defaults, but you can adjust)
# -----------------------------------------------------------------------------

# Port the service runs on
PORT=8094

# Number of virtual channels to create (eplus1 through eplusN)
LANES=40

# How many hours of programming to keep in the guide
VALID_HOURS=72

# -----------------------------------------------------------------------------
# Chrome Capture (what port is CC running on)
# -----------------------------------------------------------------------------

CC_PORT=5589
M3U_GROUP_TITLE='ESPN+ VC;

# -----------------------------------------------------------------------------
# Advanced Settings (don't change unless you know what you're doing)
# -----------------------------------------------------------------------------

# Align program start times to minute intervals (30 = :00 and :30 of each hour)
ALIGN=30

# Minimum gap between programs (in minutes)
MIN_GAP_MINS=30

# ESPN Watch API key (shared key for the project - do not change)
WATCH_API_KEY=0dbf88e8-cc6d-41da-aa83-18b5c630bc5c

# FastAPI app module (leave as-is)
APP_MODULE=bin.vc_resolver:app

# -----------------------------------------------------------------------------
# Custom Paths (optional - uncomment only if you need to override defaults)
# -----------------------------------------------------------------------------

# Default paths are:
#   Database: ./data/eplus_vc.sqlite3
#   Output:   ./out
#   Logs:     ./logs
#
# Uncomment these lines only if you want to customize:
# DB=./data/eplus_vc.sqlite3
# OUT=./out
# LOGS=./logs
# VC_M3U_PATH=./out/playlist.m3u
